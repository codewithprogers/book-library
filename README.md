# book-library

Build a small library app by expanding on the 'Book' object constructor from the
previous JavaScript lesson. Book objects are stored in an array, displayed on
the page as cards or table rows, and can be added, removed, and updated through
the interface. This project practices object constructors, prototype methods,
DOM manipulation, form handling, and separating application data from display
logic.